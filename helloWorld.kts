fun main(){
    // kotlin escape characters
    // /n, /t, //, /$, /b - backspace, /r - carriage return, /', /"

//    print("hello world")
//    val a =10
//    val b = 30
////    val a =3.0f adding floats
////    val b = 4.5f
//    val sum = a.minus(b)
//    println ("The sum is $sum")
    var bicycles = 100
        do {
            bicycles--
        } while (bicycles > 50)
        println("$bicycles bicycles in the bicycle rack\n")

}
main()